import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "opart-dev.s3.us-east-1.amazonaws.com"],
  },
};

export default nextConfig;
